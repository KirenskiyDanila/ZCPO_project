{% extends 'base.html.twig' %}

{% block title %}New Photo{% endblock %}

{% block body %}
    <div class="container">
    <h1>Создать новое фото</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Добавить') }}</button>
        {{ form_end(form) }}

    <a href="{{ path('app_photo_index') }}">Назад к списку</a>
    </div>
{% endblock %}
